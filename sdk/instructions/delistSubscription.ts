/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  i64,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DelistSubscriptionInstructionAccounts = {
  /** list account */
  list: PublicKey | Pda;
  /** platform account */
  platform: PublicKey | Pda;
  /** user account */
  user: PublicKey | Pda;
  authority?: Signer;
  /** mint account of chosen token for payment */
  paymentMint: PublicKey | Pda;
  /** user's token account of chosen mint */
  authorityTokenAccount: PublicKey | Pda;
  mint: PublicKey | Pda;
  tokenAccount: PublicKey | Pda;
  metadata: PublicKey | Pda;
  masterEdition: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  metadataTokenProgram: PublicKey | Pda;
  sysvarInstructions?: PublicKey | Pda;
  associatedTokenProgram: PublicKey | Pda;
};

// Data.
export type DelistSubscriptionInstructionData = {
  discriminator: Array<number>;
  listingStartsAt: bigint;
};

export type DelistSubscriptionInstructionDataArgs = {
  listingStartsAt: number | bigint;
};

export function getDelistSubscriptionInstructionDataSerializer(): Serializer<
  DelistSubscriptionInstructionDataArgs,
  DelistSubscriptionInstructionData
> {
  return mapSerializer<
    DelistSubscriptionInstructionDataArgs,
    any,
    DelistSubscriptionInstructionData
  >(
    struct<DelistSubscriptionInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['listingStartsAt', i64()],
      ],
      { description: 'DelistSubscriptionInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [112, 206, 93, 202, 191, 70, 21, 12],
    })
  ) as Serializer<
    DelistSubscriptionInstructionDataArgs,
    DelistSubscriptionInstructionData
  >;
}

// Args.
export type DelistSubscriptionInstructionArgs =
  DelistSubscriptionInstructionDataArgs;

// Instruction.
export function delistSubscription(
  context: Pick<Context, 'identity' | 'programs'>,
  input: DelistSubscriptionInstructionAccounts &
    DelistSubscriptionInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'dplProtocol',
    'ywpMZZNG3Nx1Bu2deJCcNxzUUoWSm6YwN9r9jCF8art'
  );

  // Accounts.
  const resolvedAccounts: ResolvedAccountsWithIndices = {
    list: { index: 0, isWritable: true, value: input.list ?? null },
    platform: { index: 1, isWritable: false, value: input.platform ?? null },
    user: { index: 2, isWritable: false, value: input.user ?? null },
    authority: { index: 3, isWritable: true, value: input.authority ?? null },
    paymentMint: {
      index: 4,
      isWritable: false,
      value: input.paymentMint ?? null,
    },
    authorityTokenAccount: {
      index: 5,
      isWritable: true,
      value: input.authorityTokenAccount ?? null,
    },
    mint: { index: 6, isWritable: true, value: input.mint ?? null },
    tokenAccount: {
      index: 7,
      isWritable: true,
      value: input.tokenAccount ?? null,
    },
    metadata: { index: 8, isWritable: true, value: input.metadata ?? null },
    masterEdition: {
      index: 9,
      isWritable: true,
      value: input.masterEdition ?? null,
    },
    systemProgram: {
      index: 10,
      isWritable: false,
      value: input.systemProgram ?? null,
    },
    tokenProgram: {
      index: 11,
      isWritable: false,
      value: input.tokenProgram ?? null,
    },
    metadataTokenProgram: {
      index: 12,
      isWritable: false,
      value: input.metadataTokenProgram ?? null,
    },
    sysvarInstructions: {
      index: 13,
      isWritable: false,
      value: input.sysvarInstructions ?? null,
    },
    associatedTokenProgram: {
      index: 14,
      isWritable: false,
      value: input.associatedTokenProgram ?? null,
    },
  };

  // Arguments.
  const resolvedArgs: DelistSubscriptionInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.sysvarInstructions.value) {
    resolvedAccounts.sysvarInstructions.value = publicKey(
      'Sysvar1nstructions1111111111111111111111111'
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDelistSubscriptionInstructionDataSerializer().serialize(
    resolvedArgs as DelistSubscriptionInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
